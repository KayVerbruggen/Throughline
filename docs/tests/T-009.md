---
id: T-009
title: Derive a sequence diagram with actor lifelines
trace:
  - R-014
result: pass
file: src/model/sequenceDiagram.test.ts
created: "2026-07-15"
---

Asserts that `deriveSequenceDiagram` makes the primary actor the first lifeline
(resolved to a stakeholder by id or title), has the actor initiate the first message
and components hand off thereafter, honours an explicit activity `initiator`, and
emits an `alt` fragment for a guarded alternate (a `loop` when it rejoins backward).
