---
id: N-003
title: A model an LLM can read and write reliably
status: approved
moscow: must
stakeholder: SH-002
tags:
  - llm
created: "2026-07-14"
---

An agent needs the model to be predictable enough to author directly: a stable file layout, obvious id conventions, and forgiving parsing so a slightly-off file still loads. The format has to be something a language model can produce correctly without running the application.
