---
id: "C-004"
title: "Downstream Gate"
parent: "C-002"
activities:
  - id: "ACT-012"
    label: "Open downstream gate"
    pre: "interlockController.chamberLevel == lower && upstreamGate.state == closed"
    effects:
      - "downstreamGate.state := open"
  - id: "ACT-023"
    label: "Close downstream gate"
    effects:
      - "downstreamGate.state := closed"
variables:
  - id: "VAR-004"
    name: "state"
    type: "enum"
    values:
      - "open"
      - "closed"
    initial: "closed"
created: "2023-02-01"
---

The gate between the chamber and the lower reach. Opens only once the interlock controller has confirmed the chamber is level with the lower reach.
